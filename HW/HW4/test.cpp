#include <iostream>
#include <string.h>
#include <vector>
using namespace std;

int main() {

    string input = "Iaa is very sad";
    pair<int, int> p;
    p = make_pair(3,2);

    // int pos = input.find(' ');
    // cout << input.substr(0, pos);

    int arr[5] = {100};

    cout << arr[0];
}