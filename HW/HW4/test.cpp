#include <iostream>
#include <string.h>
#include <vector>
#include <stack>
using namespace std;

int main() {

    string input = "Iaa is very sad";
    string noSpace = "YouAreDead";
    pair<int, int> p;
    stack<int> path;
    
    // p = make_pair(3,2);

    // path.push(3);
    // path.push(2);
    // path.push(1);

    // path.pop();
    // input << to_string(path.top());

    // int p = noSpace.find(' ');
    cout << noSpace.find(' ');
}